import ProjectCards from "./ProjectCards";

const Projects = () => {
  return (
    <section className="projects" id="projects">
      <ProjectCards />
    </section>
  );
};

export default Projects;
